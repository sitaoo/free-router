.env
.env.local
# The only writable dir at runtime (overlay, state, .env, logs).
data/
# Strays from the old flat layout; belong in data/.
router.log
router.pid
discovered-free-models.json
node_modules/
