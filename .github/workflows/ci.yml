name: CI

on:
  push:
    branches: [main, plus, 'feat/**', 'fix/**']
  pull_request:

jobs:
  check-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm run check
      - run: npm test
        timeout-minutes: 15
