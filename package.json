{
  "name": "openrouter-free-router",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ordered, health-aware TokenRouter and OpenRouter free-model gateway for Hermes Agent",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check test-smoke.mjs",
    "test": "node test-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
