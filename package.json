{
  "name": "free-router",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OpenAI-compatible local gateway that routes to TokenRouter and OpenRouter free models",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check test-smoke.mjs",
    "test": "node test-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
