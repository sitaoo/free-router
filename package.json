{
  "name": "free-router",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OpenAI-compatible local gateway that ranks free models across pluggable providers",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check providers.mjs && node --check proxy.mjs && node --check server.mjs && node --check test-smoke.mjs",
    "test": "node test-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
